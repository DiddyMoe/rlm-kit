{
  "mcp.servers": {
    "rlm-gateway": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "scripts/rlm_mcp_gateway.py"
      ],
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  }
}